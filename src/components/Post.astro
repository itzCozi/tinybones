---
import { formatDate, calculateReadingTime } from "@/lib/util";

const { post } = Astro.props;
const readingTime = calculateReadingTime(post.body || '');
---

<li>
  <a
    href={`/blog/${post.id}`}
    class="flex w-full flex-col sm:flex-row sm:items-center sm:justify-between"
  >
    <span class="link">{post.data.title}</span>
    <div
      class="text-muted-text dark:text-dark-muted-text text-sm font-normal flex flex-col sm:flex-row sm:gap-3 sm:items-center"
    >
      <span>{formatDate(post.data.publicationDate)}</span>
      <span>{readingTime}</span>
    </div>
  </a>
</li>
